// main.js

import { addRowTable,addRow, deleteRow, getValue } from './form.js';

document.getElementById('addRowBtn').addEventListener('click', addRowTable);
// document.getElementById('submitRelaysBtn').addEventListener('click', getValue);